export const environment = {
  production: false,
  // Empty = same-origin; the dev server proxies /api to the Node backend
  // (see proxy.conf.json) so this stays empty in both dev and prod.
  apiBase: '',
};
